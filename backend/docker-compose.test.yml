services:
  loadgen:
    build:
      context: .
      dockerfile: Dockerfile.loadgen
    container_name: trading-loadgen
    cpuset: "0"        
    mem_limit: 1g
    extra_hosts:
      - "host.docker.internal:host-gateway"


